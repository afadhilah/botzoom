# Production Environment Configuration
# Server: 10.28.85.33

# ============================================================
# App Configuration
# ============================================================
APP_NAME=Meeting Transcript & Zoom Bot API
APP_VERSION=2.0.0
DEBUG=False

# ============================================================
# Database Configuration (Change to PostgreSQL for production)
# ============================================================
# SQLite (current - not recommended for production)
DATABASE_URL=sqlite:///./meeting_transcript.db

# PostgreSQL (recommended for production - uncomment and configure)
# DATABASE_URL=postgresql://botzoom_user:secure_password@localhost:5432/botzoom_db

# ============================================================
# JWT & Authentication
# ============================================================
JWT_SECRET_KEY=CHANGE_THIS_TO_RANDOM_SECURE_KEY_IN_PRODUCTION_$(openssl rand -hex 32)
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================
# Security Configuration
# ============================================================
PASSWORD_MIN_LENGTH=8
OTP_EXPIRE_MINUTES=10
OTP_LENGTH=6

# ============================================================
# CORS Configuration - Update with actual frontend URL
# ============================================================
CORS_ORIGINS=["http://10.28.85.33", "http://localhost:5173"]
ALLOWED_HOSTS=["10.28.85.33", "localhost"]


# ============================================================
# AI/ML Model Configuration
# ============================================================
WHISPER_MODEL=medium
USE_GPU=True
GPU_MEMORY_FRACTION=0.9


# ============================================================
# Email Configuration (SMTP)
# ============================================================
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=your-email@gmail.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_FROM_NAME=Meeting Transcript System
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True
